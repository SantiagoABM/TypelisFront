import { FiltroActoresPipe } from './filtro-actores.pipe';

describe('FiltroActoresPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroActoresPipe();
    expect(pipe).toBeTruthy();
  });
});
