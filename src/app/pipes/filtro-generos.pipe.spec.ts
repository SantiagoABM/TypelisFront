import { FiltroGenerosPipe } from './filtro-generos.pipe';

describe('FiltroGenerosPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroGenerosPipe();
    expect(pipe).toBeTruthy();
  });
});
