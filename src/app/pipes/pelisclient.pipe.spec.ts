import { PelisclientPipe } from './pelisclient.pipe';

describe('PelisclientPipe', () => {
  it('create an instance', () => {
    const pipe = new PelisclientPipe();
    expect(pipe).toBeTruthy();
  });
});
